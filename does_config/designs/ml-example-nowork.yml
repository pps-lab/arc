first_exp:
  n_repetitions: 1
  host_types:
    server:
      n: 1
      check_status: True
      init_roles: 
        - ml-example-setup-1
        - setup-poetry
      $CMD$:
        - "sleep 10 && cd {{exp_code_dir}} && ls -al"

    server2:
      n: 2
      check_status: True
      init_roles: 
        - ml-example-setup-1
        - setup-poetry

      $CMD$:
        - "sleep 10 && cd {{exp_code_dir}} && ls -al"
        - "sleep 10 && cd {{exp_code_dir}} && ls -al"
      
