first_exp:
  n_repetitions: 1
  host_types:
    server:
      n: 1
      $CMD$: "echo \"Hello World\" && sleep 10"
      init_roles: setup-test01
  base_experiment:
    factor_1:
      $FACTOR$: [10]
    
